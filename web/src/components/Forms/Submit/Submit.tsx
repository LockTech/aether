import { Button } from '@chakra-ui/react'

/**
 * Chakra UI's {@link Button} component, with a `type` of `'submit'`.
 */
const Submit = Button

Submit.defaultProps = {
  type: 'submit',
}

export default Submit
